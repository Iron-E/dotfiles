xkb_keymap {
	xkb_keycodes {
		include "evdev"
		include "aliases(qwerty)"
	};

	xkb_types {
		include "complete"
	};

	xkb_compatibility {
		include "complete"
	};

	xkb_symbols {
		include "pc"
		include "us"
		include "inet(evdev)"
		replace key <INS> { [ Multi_key ] };
	};
};
